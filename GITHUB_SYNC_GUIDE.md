# ☁️ GitHub Sync - Руководство по настройке

## 🎯 Что это?

**GitHub Sync** - это функция автоматической синхронизации данных между всеми вашими устройствами через GitHub.

### Преимущества:

- ✅ **Автоматическая синхронизация** - данные синхронизируются каждые 10 секунд
- ✅ **Работает на всех устройствах** - телефон, планшет, компьютер
- ✅ **Всегда актуальные данные** - загружается последняя версия при открытии
- ✅ **Бесплатно** - использует ваш GitHub репозиторий
- ✅ **Безопасно** - токен хранится локально, данные в приватной ветке

---

## 📝 Настройка (5 минут)

### Шаг 1: Создайте Personal Access Token

1. Откройте: https://github.com/settings/tokens/new
2. Заполните форму:
   - **Note:** `Hamptown Checklists Sync`
   - **Expiration:** `90 days` (или больше)
   - **Select scopes:** ✅ `repo` (полный доступ к репозиториям)
3. Нажмите **"Generate token"**
4. **ВАЖНО:** Скопируйте токен сразу! Он показывается только один раз.

### Шаг 2: Активируйте синхронизацию

1. Откройте чек-лист: https://koml56.github.io/hamptown-checklists/prep-checklist.html
2. Нажмите кнопку **"☁️ GitHub Sync: OFF"**
3. Появится инструкция - нажмите OK
4. Вставьте скопированный токен
5. Готово! Кнопка изменится на **"☁️ GitHub Sync: ON"**

### Шаг 3: Используйте на других устройствах

1. Откройте чек-лист на другом устройстве
2. Нажмите **"☁️ GitHub Sync: OFF"**
3. Вставьте **тот же токен**
4. Данные автоматически загрузятся!

---

## 🔄 Как это работает

### Автоматическая синхронизация:

```
1. Вы редактируете чек-лист
   ↓
2. Локальное автосохранение (2 сек)
   ↓
3. GitHub sync (10 сек)
   ↓
4. Данные загружены в GitHub
   ↓
5. Другие устройства получают обновление
```

### Что синхронизируется:

- ✅ Все изменения в таблицах
- ✅ Добавленные/удаленные категории
- ✅ Добавленные/удаленные позиции
- ✅ Весь редактированный текст

### Где хранятся данные:

Данные сохраняются в вашем GitHub репозитории:
- **Репозиторий:** `Koml56/hamptown-checklists`
- **Ветка:** `data` (отдельная ветка для данных)
- **Файл:** `prep-checklist-data.json`

---

## 💡 Сценарии использования

### Сценарий 1: Работа дома и на работе

**Дома:**
1. Включите GitHub Sync
2. Заполните чек-лист
3. Данные автоматически сохранятся в GitHub

**На работе:**
1. Включите GitHub Sync (тот же токен)
2. Данные автоматически загрузятся
3. Продолжайте работу - всё синхронизируется

### Сценарий 2: Мобильное приложение

**На компьютере:**
- Создайте чек-лист
- GitHub Sync сохранит в облако

**На телефоне:**
- Откройте сайт в браузере
- Включите GitHub Sync
- Просматривайте актуальные данные

### Сценарий 3: Командная работа

**Менеджер:**
- Создает чек-лист
- Включает GitHub Sync

**Сотрудники:**
- Используют тот же токен (или создают свой с доступом к репозиторию)
- Видят актуальные данные
- Могут вносить изменения

---

## 🔒 Безопасность

### Где хранится токен:

- 💾 **localStorage браузера** - только на вашем устройстве
- ❌ **НЕ отправляется на сервер**
- ❌ **НЕ виден другим пользователям**

### Рекомендации:

1. **Не делитесь токеном** с посторонними
2. **Используйте срок действия** 90 дней (автоматически истечет)
3. **Удаляйте токены** которые не используете: https://github.com/settings/tokens
4. **Для команды** создайте отдельный аккаунт или используйте GitHub Organization

---

## ⚙️ Технические детали

### Частота синхронизации:

- **Локальное автосохранение:** каждые 2 секунды
- **GitHub sync:** каждые 10 секунд после последнего изменения
- **Загрузка при открытии:** автоматически

### Разрешение конфликтов:

При открытии страницы сравниваются временные метки:
- Если GitHub данные **новее** → загружаются
- Если локальные данные **новее** → используются локальные
- Синхронизация происходит автоматически

### API запросы:

Используется GitHub Contents API:
- **GET:** загрузка данных
- **PUT:** сохранение данных
- **Формат:** JSON с Base64 кодированием

---

## 🛠️ Управление

### Включить синхронизацию:

```
Нажмите "☁️ GitHub Sync: OFF"
→ Введите токен
→ Статус изменится на "ON"
```

### Выключить синхронизацию:

```
Нажмите "☁️ GitHub Sync: ON"
→ Синхронизация отключена
→ Данные остаются локально
```

### Изменить токен:

```
1. Выключите синхронизацию
2. Откройте консоль (F12)
3. Выполните: localStorage.removeItem('github-token')
4. Включите синхронизацию снова
5. Введите новый токен
```

### Проверить статус:

Откройте консоль браузера (F12) и увидите:
```
✅ Автосохранение выполнено: 14:32:15
✅ GitHub sync успешно: 14:32:25
```

---

## ❓ FAQ

**Q: Нужен ли интернет?**
A: Да, для синхронизации с GitHub нужен интернет. Без интернета работает только локальное автосохранение.

**Q: Можно ли работать оффлайн?**
A: Да! Локальное автосохранение работает всегда. При подключении к интернету данные синхронизируются.

**Q: Что если токен истек?**
A: Вы увидите ошибку синхронизации. Создайте новый токен и обновите его в настройках.

**Q: Сколько данных можно сохранить?**
A: GitHub файлы до 100 МБ. Чек-лист занимает ~20-30 КБ, этого хватит на тысячи записей.

**Q: Безопасно ли хранить токен в браузере?**
A: Токен хранится в localStorage (как cookies). Для максимальной безопасности:
- Используйте токен с минимальными правами (только repo)
- Устанавливайте срок действия
- Не используйте на чужих компьютерах

**Q: Можно ли использовать на нескольких устройствах одновременно?**
A: Да! Используйте один токен на всех устройствах. Последние изменения всегда выигрывают.

**Q: Что делать при ошибке синхронизации?**
A:
1. Проверьте интернет
2. Проверьте срок действия токена
3. Проверьте права токена (должен быть `repo`)
4. Посмотрите консоль (F12) для деталей ошибки

---

## 🔧 Устранение проблем

### Ошибка: "401 Unauthorized"

**Причина:** Неверный токен или истек срок действия

**Решение:**
1. Создайте новый токен
2. Выключите GitHub Sync
3. Удалите старый токен из localStorage
4. Включите GitHub Sync с новым токеном

### Ошибка: "404 Not Found"

**Причина:** Ветка `data` не существует

**Решение:**
Ветка создается автоматически при первом сохранении. Если проблема сохраняется, создайте вручную:
```bash
git checkout -b data
git push origin data
```

### Ошибка: "403 Forbidden"

**Причина:** Недостаточно прав у токена

**Решение:**
Пересоздайте токен с правом `repo` (полный доступ к репозиториям)

### Данные не синхронизируются

**Проверьте:**
1. GitHub Sync включен (кнопка показывает ON)
2. Есть интернет
3. В консоли (F12) нет ошибок
4. Токен действителен

---

## 📚 Дополнительные ресурсы

- [GitHub Personal Access Tokens](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens)
- [GitHub Contents API](https://docs.github.com/en/rest/repos/contents)
- [localStorage Web API](https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage)

---

## 🚀 Roadmap

Планируется:
- [ ] Поддержка GitHub Organizations для команд
- [ ] Конфликт-резолвер с визуальным сравнением
- [ ] История изменений (версионирование)
- [ ] Экспорт резервных копий в отдельную ветку
- [ ] Уведомления о синхронизации

---

Made with ❤️ for Hamptown Burger
Powered by GitHub API
