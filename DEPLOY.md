# 🚀 Инструкция по публикации на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [GitHub](https://github.com)
2. Нажмите кнопку "New repository" (зеленая кнопка)
3. Заполните форму:
   - **Repository name:** `hamptown-checklists`
   - **Description:** `Interactive checklist system for Hamptown Burger restaurant`
   - **Visibility:** Public (или Private, если хотите ограничить доступ)
   - ❌ **НЕ** ставьте галочку "Add a README file"
   - ❌ **НЕ** добавляйте .gitignore
   - ❌ **НЕ** выбирайте лицензию
4. Нажмите "Create repository"

## Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните следующие команды в папке проекта:

```bash
cd C:\Users\kovl\hamptown-checklists

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваше имя пользователя)
git remote add origin https://github.com/YOUR_USERNAME/hamptown-checklists.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

### Пример с конкретным именем пользователя:
Если ваш username на GitHub `johndoe`, команда будет:
```bash
git remote add origin https://github.com/johndoe/hamptown-checklists.git
git branch -M main
git push -u origin main
```

## Шаг 3: Активируйте GitHub Pages

1. Перейдите в настройки репозитория: Settings
2. В левом меню выберите "Pages"
3. В разделе "Source" выберите:
   - **Branch:** `main`
   - **Folder:** `/ (root)`
4. Нажмите "Save"
5. Подождите 1-2 минуты

## Шаг 4: Проверьте работу сайта

После активации GitHub Pages, ваш сайт будет доступен по адресу:
```
https://YOUR_USERNAME.github.io/hamptown-checklists/
```

Пример:
```
https://johndoe.github.io/hamptown-checklists/
```

## 🔄 Обновление проекта

После внесения изменений в файлы:

```bash
cd C:\Users\kovl\hamptown-checklists

# Добавить все изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push
```

## 🔐 Аутентификация

При первом `git push` GitHub может запросить аутентификацию:

### Вариант 1: HTTPS (рекомендуется)
Используйте **Personal Access Token** вместо пароля:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите срок действия и permissions: `repo` (полный доступ к репозиториям)
4. Скопируйте токен (он показывается только один раз!)
5. При запросе пароля вставьте токен

### Вариант 2: SSH
Если у вас настроен SSH ключ, используйте SSH URL:
```bash
git remote set-url origin git@github.com:YOUR_USERNAME/hamptown-checklists.git
```

## 📱 Доступ к сайту

После успешной публикации:

**URL главной страницы:**
```
https://YOUR_USERNAME.github.io/hamptown-checklists/
```

**Прямые ссылки на чек-листы:**
```
https://YOUR_USERNAME.github.io/hamptown-checklists/prep-checklist.html
https://YOUR_USERNAME.github.io/hamptown-checklists/inventory-checklist.html
```

## ✅ Проверка статуса публикации

1. Перейдите в репозиторий на GitHub
2. Нажмите "Actions" (если есть)
3. Или проверьте Settings → Pages - там будет ссылка на опубликованный сайт

## 🎉 Готово!

Теперь вы можете:
- ✅ Делиться ссылкой с коллегами
- ✅ Открывать сайт на любом устройстве
- ✅ Вносить изменения и автоматически обновлять сайт через git push

## 🆘 Решение проблем

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/hamptown-checklists.git
```

### Ошибка: "Updates were rejected"
```bash
git pull origin main --rebase
git push origin main
```

### GitHub Pages не работает (404)
1. Проверьте, что в репозитории есть файл `index.html` в корне
2. Подождите 5-10 минут после первой активации Pages
3. Очистите кеш браузера (Ctrl+F5)
4. Проверьте Settings → Pages - должен быть зеленый чекбокс

### Изменения не появляются на сайте
- GitHub Pages обновляется с задержкой 1-2 минуты
- Очистите кеш браузера (Ctrl+Shift+R или Ctrl+F5)
- Проверьте, что вы сделали `git push`

---

**Нужна помощь?**
- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [Git Documentation](https://git-scm.com/doc)
